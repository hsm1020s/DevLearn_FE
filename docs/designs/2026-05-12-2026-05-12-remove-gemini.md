# 설계: 2026-05-12-remove-gemini

**생성:** 2026-05-12 13:03
**워크트리:** /Users/moon/DevLearn_FE_wt/2026-05-12-2026-05-12-remove-gemini
**브랜치:** task/2026-05-12-2026-05-12-remove-gemini

## 목표
LLM 선택 옵션에서 Gemini를 제거하고, 관련 클라이언트/설정 코드를 지운다.
이유: 실제 서비스에서 Gemini 호출 비중이 낮고 API 키 관리 비용만 발생해 유지 가치가 없음.
사용자 결정으로 Gemini 모델 옵션을 영구 제거.

## 변경 범위

### 프론트엔드 (`/Users/moon/DevLearn_FE`)
- `src/utils/constants.js` — `LLM_OPTIONS` 배열에서 `gemini-2.5-flash-lite` 항목 삭제.

### 백엔드 (`/Users/moon/IdeaProjects/DevLearn_BE`)
- `src/main/java/com/moon/devlearn/chat/service/llm/GeminiProvider.java` — 파일 삭제.
- `src/main/java/com/moon/devlearn/config/LlmConfig.java` — `gemini` ProviderProps 필드 및 JavaDoc 정리.
- `src/main/resources/application.yml` / `application-local.yml` — `llm.gemini` 블록 제거.
- `src/test/resources/application-test.yml` — `llm.gemini` 블록 제거.
- `src/test/java/com/moon/devlearn/chat/LlmClientTest.java` — gemini 관련 단정 제거.
- JavaDoc에 남은 "Gemini" 예시 텍스트 정리(`ChatRequest`, `LlmClient`, `LlmProvider`, `StudyService`).

## 구현 계획
1. FE: `LLM_OPTIONS`에서 gemini 라인 1줄 제거 + 주석 "클라우드 3종 → 2종" 표현 정리.
2. BE: `GeminiProvider.java` 삭제 → 컴파일러가 의존성 잡아주면 LlmConfig/yml/test/JavaDoc 정리.
3. 빌드/테스트로 잔존 참조 없는지 확인.

## 단위 테스트 계획
- FE: 모델 선택 드롭다운에 Gemini가 더이상 노출되지 않는지 UI 확인.
- BE: `./gradlew test` 통과(특히 `LlmClientTest`).
- BE: `application.yml` 파싱 시 unknown property 경고 없는지 부팅 로그 확인.

## 회귀 테스트 계획
- 채팅 모드에서 GPT / Claude / 로컬 모델 중 1개로 메시지 송수신 → 응답 정상 출력 확인.
- 마인드맵 또는 문서 업로드 기능 1개 동작 확인(LLM 변경이 다른 도메인을 깨지 않았는지).
