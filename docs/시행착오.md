# 시행착오 기록

이전 구현을 **구조적으로 갈아엎고 다시 만든 경우**를 남기는 문서.
단순 버그 수정·리팩터·스타일 개선은 [WORK_LOG.md](WORK_LOG.md) 에 기록하고, 여기엔 적지 않는다.

## 엔트리 기준
- 같은 기능을 아키텍처/흐름 단위로 **다시 구현**한 경우
- `git revert` 또는 코드 대부분 교체
- 기존 구현 방향을 포기하고 다른 접근으로 전환

## 템플릿
```md
## YYYY-MM-DD — <제목>
- **기존 구현:** `<hash>` <커밋 메시지 첫 줄>
- **새 구현:** `<hash>` <커밋 메시지 첫 줄>
- **이유:** <왜 갈아엎었는지 — 기술·UX·제약 등>
- **잔존 이슈:** <데드 파일/후속 정리 대상 등, 없으면 생략>
```

---

## 2026-04-22 — 파인만 학습 모드 아키텍처 변경
- **기존 구현:** `871e5d09` feat(feynman): PDF 기반 파인만 학습 모드 추가
- **새 구현:** `e0b862b6` refactor(feynman): 별도 모드에서 자격증 모드 내 스타일 칩으로 통합
- **이유:** 별도 메인 모드로 분리하면 기존 학습 대화 UX와 단절되고, 챕터 선택 UI가 메인 모드 전환 단계에 끼어 번거로움. 자격증 모드 안의 스타일 칩으로 편입해 기존 대화 흐름·스타일 고정·SSE 스트리밍 재사용.
- **잔존 이슈:** `FeynmanMode.jsx`(레거시 shim) / `FeynmanWorkspace.jsx` / `FeynmanResult.jsx` 모두 미참조 데드 파일 — 추후 정리 예정
