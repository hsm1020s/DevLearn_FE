# TODO — 실서비스 완성을 위한 작업 목록

> 우선순위: P0(즉시) → P1(핵심) → P2(개선) → P3(나중에)
> 완료 시 체크 표시하고 날짜 기록

## P1 — 핵심 기능 (실서비스에 필수)

### 다크 모드
- [ ] 다크 모드 토글 UI 추가 (CSS 변수는 이미 준비됨, 전환 UI만 필요)
- [ ] 사이드바 또는 설정에 토글 배치, localStorage로 선호 테마 저장

### 대화 검색
- [ ] 사이드바에 대화 검색 입력창 추가 (대화명 필터링)
- [ ] 대화 20개 이상일 때 스크롤만으로는 사용 불가

### 메시지 편집 / 재생성
- [ ] 사용자 메시지 편집 → 해당 시점부터 재생성
- [ ] assistant 메시지에 "다시 생성" 버튼

### 에러 바운더리
- [ ] 메인 콘텐츠 영역에 ErrorBoundary 추가 (렌더 에러 시 백지 화면 방지)
- [ ] 마인드맵 캔버스에 별도 ErrorBoundary (ReactFlow 에러 격리)
- [ ] 네트워크 오프라인 상태 감지 및 안내 UI

### 퀴즈 유형 확장
- [ ] OX 문제 UI 렌더링 (현재 객관식만 동작)
- [ ] 단답형 문제 UI 렌더링 (텍스트 입력)
- [ ] 퀴즈 챕터를 PDF에서 실제 추출된 데이터로 표시 (현재 하드코딩)

### 반응형 레이아웃
- [ ] 모바일(768px 미만) 레이아웃 대응
- [ ] 사이드바 접힘 시 아이콘 버튼으로 모드 전환/새 채팅 가능하도록

## P2 — UX 개선 (사용 품질 향상)

### 로딩/스켈레톤
- [ ] 모드 전환 시 Suspense fallback을 모드별 스켈레톤 UI로 교체
- [ ] 스트리밍 시작 전 타이핑 인디케이터 (점 3개 애니메이션)

### 대화 관리 강화
- [ ] 대화 자동 제목 생성 시 단어 경계에서 자르기 (현재 30자 기계적 절단)
- [ ] 즐겨찾기 3개 제한 → 제한 없이 or 제한 안내 UI
- [ ] LLM 모델을 대화별로 저장/전환 (현재 전역 1개)

### 마인드맵 강화
- [ ] PDF 내보내기 시 fitView 후 전체 캔버스 캡처 (현재 뷰포트만 캡처)
- [ ] 마인드맵 노드에 메모/설명 추가 기능
- [ ] 마인드맵 간 노드 복사/이동

### 학습 이력
- [ ] 퀴즈 누적 학습 이력 저장 (세션별이 아닌 전체 기간)
- [ ] 오답 노트 자동 생성
- [ ] 학습 통계 시각화 (일별/주별 차트)

### 키보드 단축키
- [ ] `Cmd/Ctrl+K` — 대화 검색
- [ ] `Escape` — 모달/팝오버 닫기 통합
- [ ] 사이드바 컨텍스트 메뉴 키보드 탐색

## P3 — 부가 기능 (차별화)

### 접근성(A11y)
- [ ] 스트리밍 출력에 `aria-live="polite"` 추가
- [ ] 퀴즈 정답/오답에 텍스트 라벨 추가 (색맹 대응)
- [ ] `index.html`에 `lang="ko"` 추가
- [ ] 컨텍스트 메뉴 키보드 접근성

### 내보내기/공유
- [ ] 대화 내용 마크다운 내보내기
- [ ] 마인드맵 이미지(PNG) 내보내기
- [ ] 대화/마인드맵 공유 링크 (읽기 전용)

### 기타
- [ ] 404 페이지 추가 (현재 알 수 없는 경로 진입 시 빈 화면)
- [ ] 제안 모달 데이터를 서버로 전송 (현재 localStorage에만 저장)
- [ ] lazy() 호출을 모듈 레벨로 이동 (모드 전환 시 불필요한 언마운트 방지)
- [ ] FileDropZone에 비PDF 파일 드롭 시 즉시 안내 메시지
