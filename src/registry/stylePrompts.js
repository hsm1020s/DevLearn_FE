/**
 * @fileoverview 학습 모드 채팅 스타일별 LLM 프롬프트 템플릿.
 *
 * 사용자가 스타일 칩(파인만 등)을 선택하면, 여기서 정의한 지시문이 사용자 메시지
 * 앞에 붙어 LLM에 전달된다. 프롬프트를 수정·추가할 때 이 파일만 편집하면 된다.
 *
 * 규칙:
 *   - key는 CHAT_STYLES(constants.js)의 value와 일치해야 한다.
 *   - 'general'은 프리픽스 없이 원문 그대로 전송하므로 여기에 정의하지 않는다.
 *   - Mock 호환: 지시문 첫 토큰은 '[학습 모드 지시문]'으로 시작해야
 *     chatMock.js의 detectStyle이 스타일을 인식한다.
 */

export const STYLE_PROMPT = {
  feynman:
    '[학습 모드 지시문] 아래 사용자 메시지에 대해 "파인만 기법"으로 응답해줘. ' +
    '즉, 사용자가 개념을 스스로 설명했다면 빠진 부분·잘못 이해한 부분을 짚어주고, ' +
    '설명을 요청한 경우 아이에게 가르치듯 단계별 쉬운 비유로 풀어서 설명해줘.\n\n' +
    '사용자 메시지:\n',
};
